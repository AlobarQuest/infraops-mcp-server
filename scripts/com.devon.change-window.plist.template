<?xml version="1.0" encoding="UTF-8"?>
<!-- Template for the daily change-window LaunchAgent. Install with scripts/install-change-window-launchd.sh,
     which substitutes __REPO__ / __HOME__ and writes ~/Library/LaunchAgents/com.devon.change-window.plist.
     No secrets live here — the wrapper sources them from ~/.config/infra-drift/env (gitignored). -->
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.devon.change-window</string>

    <key>ProgramArguments</key>
    <array>
        <string>/bin/bash</string>
        <string>__REPO__/scripts/change-window.sh</string>
    </array>

    <key>StartCalendarInterval</key>
    <dict>
        <key>Hour</key>
        <integer>4</integer>
        <key>Minute</key>
        <integer>0</integer>
    </dict>

    <key>EnvironmentVariables</key>
    <dict>
        <key>PATH</key>
        <string>/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin</string>
        <key>HOME</key>
        <string>__HOME__</string>
    </dict>

    <key>StandardOutPath</key>
    <string>__HOME__/Library/Logs/change-window-launchd.log</string>
    <key>StandardErrorPath</key>
    <string>__HOME__/Library/Logs/change-window-launchd.log</string>

    <key>RunAtLoad</key>
    <false/>
</dict>
</plist>
